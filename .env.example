# AMDSF Environment Configuration
# Add your external service API keys here if available.
# The system runs with graceful fallbacks and demo simulation if keys are omitted.

NASA_API_KEY=DEMO_KEY
LEOLABS_API_KEY=
ENVIRONMENT=development
PORT=8000
